{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "npm run prettier && lerna run test",
    "publish": "lerna publish",
    "prettier": "prettier . --check",
    "prettier:fix": "prettier . --check --write"
  },
  "devDependencies": {
    "from-cwd": "^1.0.1",
    "glob": "^7.1.6",
    "lerna": "^3.22.1",
    "prettier": "^2.1.2"
  }
}
